显示GPU列表
显示系统里安装的显示GPU列表，并可选择主要GPU，所有在主窗口所做的调整都会应用在主GPU上，也可以选择性的应用在系统中所有类似的GPU上，如果GPU设定同步选项已启动的话。


提示:
- 在选择新的主GPU之前，永远记得在主窗口中先应用所有更改，任何未应用的更新在选择新的主GPU之后都会流失
- 你可以在“设定” -> “用户界面”选项卡中取消勾选来关闭用户界面提示