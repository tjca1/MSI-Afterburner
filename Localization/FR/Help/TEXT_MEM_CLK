Horloge de mémoire 
Affiche l'horloge mémoire du GPU maître. Dépendant du modèle de carte graphique la clock peut-être représentée soit explicitement soit par un offset par rapport à la clock de base. Les valeurs signées représentent un offset tandis que les valeurs non signées représentent une clock absolue.

Astuce:
- Quand le paramètre mode de syncronisation GPU est activé dans la tabulation <Générale> dans les propiétés avancées, tout changement appliqué au GPU maître est également appliqué à tous les GPU similaires installé dans le système.
- Vous pouvez désactiver les bulles d'interface dans la tabulation <interface d'utilisateur> dans les propriétés avancées.