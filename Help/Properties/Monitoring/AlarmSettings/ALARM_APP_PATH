Alarm application path
Displays and allows editing path for external applicaiton to be launched on alarm. 

Hints:
- This option is affected by adjacent alarm timeout settings.
- You may use macro variables in the path. Use %WinDir% to specify Windows directory, %SysDir% to specify Windows\System32 directory, %ABDir% to specify %PRODUCTNAME% installation directory or %AB% to specify full path to %PRODUCTNAME% executable. 
- You may disable user interface tooltips via <User interface> tab in advanced properties.